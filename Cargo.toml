[workspace]
members = [
    "crates/aetheria-core",
    "apps/aetheria-daemon",
    "apps/aetheria-client"
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
